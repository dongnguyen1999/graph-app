import {colors} from "./color"
export const styles = {
    body: {
        flexDirection: 'row',
        justifyContent: 'center',
        backgroundColor: colors.body,
        height: 60,
        alignItems: 'center'
    },
    button: {
        color: colors.button,
        size: 50
    }
}
