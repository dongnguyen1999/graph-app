import {colors} from "./color"
export const styles = {
    circleVertexBody: {
        fill: colors.vertexBackgroundColor,
        strokeWidth: 3,
        stroke: colors.vertexBorderColor,
        zIndex: 10,
        position: 'relative'
    },
    vertexName: {
        fontWeight: 'bold',
        fontSize: 20,
    },
};
