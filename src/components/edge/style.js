import {StyleSheet, PixelRatio} from "react-native"
import {colors} from "./color"
export const styles = {
    lineBody: {
        stroke: colors.edgeColor, 
        strokeWidth:3,
        fill: "transparent"
    },
};
